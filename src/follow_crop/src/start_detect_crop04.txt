# 程序的功能应用于在学校的环境中调节PID
start_detect_crop04.cpp是在start_detect_crop02的基础上进行改进的，改变的内容：
1、原来摄像头检测不到画面程序就死了，现在不会
2、经过分析程序死亡原因是画面中没有检测到线，所以没有检测到的特征点，意思是检测到的特征点为0导致函数cv::fitLine会提示错误造成结束程序
3、所以在检测完特征点之后，对总的特征点进行判断，
   如果特征点为0则固定的输出一个角度值，现在定的角度值是0，然后把这个角度送到PID那里